# Production punchlist

Date: 2026-07-30

Baseline audited: `da6d89fe0c7997653807080bcfbebc6a00356395`

Goal: complete GLM-5.2 serving on four RTX PRO 6000 Blackwell SM120 GPUs,
TP=4 over PCIe, with EXL3/NVFP4 hybrid weights, MTP0–6, 1M context, tiered
KV, concurrent serving, quality evidence, and matched benchmarks.

This is a blocking punchlist, not an optimization backlog. The engine is not
production-capable while any item below remains open.

State meanings:

- `PASS`: current hashed evidence proves the stated scope.
- `REVIEW`: implementation/design exists but its required adversarial token
  is withheld.
- `OPEN`: required implementation or evidence is missing.
- `AUTH`: work requires renewed cn4 operator authorization.

## P0 — correctness and execution

| ID | State | Required outcome | Current evidence / blocker | Next gate |
|---|---|---|---|---|
| C01 | REVIEW | Corrected NVFP4 manifest ABI, EXL3 source projection, and EXL3 warp-decode gates accepted and byte-bound to the source actually built | Fable's three corrective r2 kernel reviews give substantive unqualified passes, but their result text omits 3, 6, and 12 handoff input hashes respectively. The repository-wide provenance gate therefore rejects all three as accepted artifacts. Their temporary root copies were removed so device scripts remain fail-closed; `docs/fable-kernel-r2-attestation-repair-request.md` lists the exact missing attestations. The protected-precision plain-padding streaming correction is separately machine-accepted. Prior manifest findings `m5` and `m7` have atomic-publication and bit-exact-scale CPU corrections awaiting review. Corrections through `3049a5a` reject resigned NVFP4 mode/reserved/global-scale lies, noncanonical zero-scale/value/scale padding, unequal 2D scale replicas, and file-backed converter/direct-loader bypass. The M4 manifest audit found one additional format contradiction: `spec/format-v0.md` requires header bit 3 for protected tensors, but `derive_header_flags` never sets it. Current-tree review-to-build binding is specified at `60311cf` and awaits design review | Obtain byte-complete Fable attestation repairs, adversarially review the protected-header correction with the M4 laboratory manifest, then re-pin the complete current-tree format/EXL3 gate before conversion |
| C02 | REVIEW | Immutable row input carries prompt IDs/positions, sampling tuple, seed/RNG, MTP posture, limits, and page-delta binding to all ranks | CPU v1 ABI `d1f9825` hashes exact row/prompt/sampling state, and integration `e1d51ce` constructs/shares/verifies it across four rank threads. It cannot represent the recurrent one-ahead MTP bundle. Coordinated design `bab7866` now freezes fixed 480-byte `StepInput.v2` rows, six target/MTP phases, current/next bundle generations, four rank-local proposal/retained-state digests, physical-step counter ownership, rank/full-device page-table digests, and split common/local output consensus. Current Rust remains v1; final RNG output/commit and device consumption are absent | Fable v3 cross-contract review, then one atomic CPU implementation/proof rather than incremental mixed-version amendments |
| C03 | REVIEW | Scheduler, prefix leases, active KV IDs, rank uploads, commit, rollback, and ID quarantine form one transaction | `f480ef1` makes the active table mandatory; `271d1f4` adds canonical deltas/mirrors; `e1d51ce` initializes four persistent mirrors and acknowledges admission, reservation, commit/rollback, and removal generations with global/rank-local digests. The atomic retryable sequence-removal correction is machine-accepted. `cd42ad4` commits accepted tentative pages in place and quarantines rejected/removed target+draft IDs until the exact four-rank successor receipt. Fable independently proved the fixed 174/128/64/64 capacities but withheld the first fixed-journal token because mirror apply remained O(context) and first-install rank staging was uncharged. Corrective design `docs/fixed-page-transaction-v1-r2.md` requires touched-suffix-only mirror work, fixed reverse-owner indexes and logical-state commitments, admission-charged final page-index blocks, and an eight-slot bounded first-install stream. Coordinated v3 design `bab7866` adds separate target/draft valid ends, four post-apply full-device table digests, private one-ahead teacher state, and the required eight-slot MTP6 reservation while retaining the 128-edit decode/verify bound. Fixed CPU storage/transactions and device upload/hash acknowledgment remain absent | Fable r2 fixed-journal and v3 reviews, then implement the accepted fixed CPU transaction before CUDA stream-visible device receipts |
| C04 | REVIEW | Active sequence page table and page-aligned C64/MTP6 arena arithmetic accepted | The atomic sequence-removal and cross-component prefix/residency coherence corrections are machine-accepted. Prior table, budget, identity, and serving corrections remain. `271d1f4` removes per-token committed append, exhausts all 448 tail/depth reservation combinations, and validates canonical deltas against bounded independent mirrors. The cache-budget r2 CPU correction derives byte terms from shared constants, rejects asymmetric or unconstructible rank arenas, validates pending floors, and permanently constructs the exact 4,160-page adversarial C64 state inside both reviewed 4,167-page MTP6 and 4,161-page MTP0 arenas. Fable proved the fixed transaction capacities exact but correctly rejected the retained clone/full-validation mirror path. The r2 amendment now makes a hidden unchanged-prefix read observable through accessor-level work counters and requires zero post-health allocation. Fixed mirror implementation remains absent | Review the static budget correction and fixed-page r2 design, then implement the CPU fixed-arena/mirror proof against the retained oracle |
| C05 | REVIEW | Persistent rank workers own real SM120 contexts, streams, graph instances, weight arenas, KV arenas, and collective handles | The first executor review withheld its token on three bounded contract gaps. Corrective candidate `a0f2bee` now creates collective resources before capture; explicitly budgets/measures collective- and graph-internal HBM, pinned-host memory, and strict free-HBM escrow; freezes the exact 16-byte-aligned owner-thread C ABI; and closes unwind, peer rollback, tier command, route latch, prepare deadline, slow-client, and pending-MTP semantics. Through `843f4ae`, four persistent native rank threads already create owner-bound CUDA contexts/streams, observe exact device identities, gate allocation on per-rank live HBM, allocate immutable weight/metadata arenas, upload and full-readback-verify checkpoint planes, resolve and reconcile all 59,585 authenticated tensor spans, and return rank-exact finalize/cleanup receipts. Graphs, KV arenas, collectives, and model-kernel execution remain absent; the native path has not been compiled or launched on SM120 at this candidate | Fable r2 executor and startup/load/binding reviews, then CPU/mock ABI/executor implementation and authorized SM120 compile/load smoke before graph/KV/collective execution |
| C06 | REVIEW | NVFP4 block-scaled MMA executes routed FC1, SwiGLU, FC2, compaction, activation quantization, epilogues, empty/skewed experts, and all required M buckets | CUDA-core and CUTLASS compile controls exist; no accepted device launch or complete fused operator. The r2 self-audit fixed seven route/layout/rounding/workspace defects, but a cross-contract audit found that its byte permutation is not generally canonical for 2D block-16x16 scale replicas. Corrective `docs/nvfp4-fused-routed-moe-v1-r3.md` retains r2's dense top-8/four-stage/compact-value/padded-SFA/slot-FMA/3,072-row fixes while limiting fused FC1 to 1D `0x1202/0x1202`; it rejects implicit 2D conversion, distinguishes exact codec variants, and binds layouts into the target program. It is an unreviewed design only | Adversarially review r3, then implement its coordinated byte-exact layout/policy/route/quantization/epilogue/workspace CPU proof; after renewed cn4 authorization, qualify the existing controls before implementing and comparing the fused candidate |
| C07 | REVIEW | Direct EXL3 gate/up/down execution consumes pinned source bytes with no unmeasured repack | Fable substantively accepted the source-mapping and warp-decode r2 corrections, but their artifacts are not yet machine-acceptable because they omit complete pinned-input attestations. The designs have an executable CPU gate at `c1ab9d2`: it independently stages source U32 words, compares all 6,291,456 weights across both real geometries, proves row/barrier coverage for M1–M8, mutation-tests tile transposition/slot/barrier errors, and matches eight-row row-major FP16 projection hashes bitwise. The CPU-proof token and all CUDA/device evidence remain absent | Repair the two r2 result attestations, obtain the CPU-proof review, then implement the separate fail-closed v2 CUDA entry point and run actual-shape correctness/inclusive timing after renewed cn4 authorization |
| C08 | REVIEW | Complete GLM target layer implements embedding, absorbed MLA/indexer, norms, residuals, routing, experts, final head, pending logits, and exact TP/DCP reductions | Source-pinned execution candidate `83f5005` and adversarial handoff define the complete program; CPU ABI/reference and CUDA layer are intentionally absent. The resident-binding proof at `a49210f` establishes the tensor-ID-to-device-span boundary. The first projection-discriminator design distinguished split EXL3 from combined NVFP4 but did not bind physical layout IDs and proposed a combined production branch that the capacity-EXL3-only manifest explicitly rejects. R3 replaces it with a 16-byte layout-bound v2 record, exact gate/up physical realization and byte charges, and explicit laboratory/hybrid manifest prerequisites; those manifests remain undesigned and unimplemented | Fable target-layer, resident-binding, and r3 verdicts; then CPU target-program proof, reviewed laboratory manifest, and one-layer TP4 replay after C06/C07 |
| C09 | REVIEW | Distributed greedy, bounded top-k/top-p, mass, residual, and bonus sampling execute without full-vocabulary gather | Contract candidate `7c71818` freezes vocabulary padding, filters, SplitMix draws, MTP proposal/accept/residual/bonus schedule, composite routes, and logical `StepOutput.v2`; correction `b235dc8` rejects globally all-masked greedy rows. Coordinated design `bab7866` makes physical ownership explicit: installed DRAFT tickets commit with one bundle, its acceptance/residual tickets continue in the later verifier, and replacement DRAFT tickets follow before final counter commit; rank-local q state is distinct from common output consensus. Wire routes, exact v3 CPU proof, and engine integration remain absent | Fable sampling, greedy, and v3 cross-contract reviews, then implement the complete CPU proof |
| C10 | REVIEW | Real recurrent GLM draft layer supports MTP0–6 proposal, verification, commit/rollback, accepted EOS, residual, and bonus semantics | Source-derived candidate `fd80e16` specifies successor-aligned teacher sidecars, recurrent scratch, the one-token pipeline, and q-state retention. Coordinated ABI candidate `bab7866` now provides explicit PREFILL, TARGET_DECODE, MTP_BOOTSTRAP, MTP_VERIFY, MTP_PIPELINED_ZERO, and MTP_FLUSH commands; materialized/emitted ends; current/next bundle identities; one-ahead page validity; and phase-exact output/counter rules. No CPU runner or model evidence exists | Fable MTP and v3 ABI reviews, then one coordinated CPU proof and matched target-only versus pipelined MTP0 equivalence |
| C11 | REVIEW | Strict four-rank checkpoint loader maps a fit-capable rank set into immutable device arenas and reaches healthy startup | Through `843f4ae`, the typed loader authenticates complete external capacity-EXL3 rank manifests and payloads, derives the 59,585-entry common catalog, and performs an all-rank prepare/adopt/finalize transaction. The production builder correctly rejects laboratory and hybrid profiles. `docs/nvfp4-laboratory-manifest-v1.md` now defines the missing separate M4 route: strict 533-tensor schema, 192-byte layout-bound catalog, exact 1,982,245,376-byte weight arena, distinct 65,536-byte file versus 130,944-byte device metadata terms, laboratory-only budget/domain/type state, and no conversion to production health. It is an unreviewed design only. No native compilation, cn4 launch, full-rank load result, model kernel, or production-health result exists | Review the consolidated production load contracts and laboratory manifest separately; implement the accepted laboratory CPU/mock builder before any M4 CUDA load, while keeping full serving startup gated by the fit-capable production path |
| C12 | OPEN | API backend serves checkpoint outputs rather than CPU worker tokens | Bounded adapter at `4cf3a62`; CPU-only and greedy-only. Correction `b22781c` prevents production-health publication before the runtime thread owns its coordinator state and synchronously cleans a pre-ready failure. Correction `f56e0bc` makes accepted-request cancellation independent of a saturated submission queue. Reviews are pending | Connect only after C02/C03/C05/C09 |

## P0 — quality

| ID | State | Required outcome | Current evidence / blocker | Next gate |
|---|---|---|---|---|
| Q01 | REVIEW | Target-only MTP0 logits match pinned reference through prefill/decode with full-vocabulary per-position retention | `docs/quality-acceptance-v1.md` freezes raw-logit identity, MPFR KLD, stable/tie classification, smoke and multi-window thresholds; handoff pins candidate `70222ab`; evaluator and model evidence are absent | Fable review of `docs/fable-quality-acceptance-v1-handoff.md`, evaluator CPU proof, then small-checkpoint target runner |
| Q02 | OPEN | Hybrid weight membership is selected from measured per-position quality and actual speed/capacity evidence | Immutable policy machinery exists; checked profile remains `conversion_allowed=false` | Calibrate EXL3/NVFP4/protected candidates after kernel evidence |
| Q03 | REVIEW | Downstream reasoning, coding, tool/JSON, repetition, and termination suite passes against pinned control | Quality candidate `70222ab` freezes the statistical gates; source candidate `83fb374` pins public tasks; generated-corpus candidate `27fa48e` now makes the 3,500 JSON/repetition/retrieval/termination cases byte- and token-implementable, but all three reviews, generator/materializer/evaluator code, gated multilingual hashes, tokenized windows, and model results remain absent | Fable quality, source, and `docs/fable-generated-quality-corpus-v1-handoff.md` reviews, then implement and prove the CPU materializer/evaluator before Q01 model execution |
| Q04 | OPEN | Frozen and randomized retrieval pass through the documented context limit | KV CPU arithmetic is not model retrieval | Execute model context-band matrix through 1,048,576 positions |
| Q05 | REVIEW | MTP1–6 output is equivalent to matched MTP0 under reviewed contract, with per-position acceptance/probability evidence | Quality candidate `70222ab` freezes per-depth stable/tie, full-vocabulary KLD, top-two error, task, and performance-enablement gates; no real draft execution exists | Fable quality review, C10, then matched greedy and stochastic evidence |

## P0 — capacity, cache, and recovery

| ID | State | Required outcome | Current evidence / blocker | Next gate |
|---|---|---|---|---|
| K01 | PASS | CPU KV/page arithmetic represents one 1,048,576-token MTP-capable sequence with balanced DCP4 ownership and explicit slack | `glm-cache` tests plus the budget/profile arithmetic at `c33648a`; the r2 correction additionally constructs the exact C64 rank-0 maximum of 4,160 pages inside both reviewed MTP0/MTP6 arenas and rejects asymmetric/unconstructible plans | Preserve while integrating C03 and obtain the scoped r2 CPU review |
| K02 | REVIEW | Real target/indexer/draft KV uses preallocated HBM arenas and qualified asynchronous HBM↔DRAM transfers | CPU byte-owning residency remains the only implementation. Design candidate `839f377` derives the exact two-plane target/four-plane MTP pitched-copy route from the adopted `[group][page][64][record]` arenas; specifies pinned target/MTP DRAM capacity, rank-partitioned dual-registered bridge buffers, host/HBM/event generations, row-digest integrity receipts, four-rank restore/demotion/publication transactions, and exact 1M physical DRAM bytes. It also identifies required unimplemented native amendments for checked 2-D copies, precreated NUMA host registration, and a target-only row-digest kernel. A newly isolated numerical-contract gap shows that the current group-scale-first NoPE reader can differ by one ULP from the historical left-associated reader while still advertising `dynamic-token-v1`; `docs/kv-reconstruction-rounding-v2.md` proposes the required explicit RN32 order, semantic ABI-v2, and cold namespace boundary before either CPU or CUDA KV work proceeds. These candidates depend on the still-pending executor-r2 design and contain no native/CUDA/device evidence | Fable review of KV rounding-v2, transfer, and executor-r2; implement the accepted KV CPU oracle first, then one pitched-memory/asynchronous-event CPU mock before native ABI and authorized SM120 qualification |
| K03 | REVIEW | Qualified DRAM↔NVMe restore/eviction works under pressure without blocking decode | The direct-tier design at `69895e0` and retained durable catalog extent/physical-EOF correction are machine-accepted. The extent CPU candidate implements exact aligned 493/501-block target/MTP layouts, complete/per-piece digests, exhaustive zero-padding rejection, and a fail-closed legacy migration boundary. The state CPU candidate adds fixed aligned buffer generations, ABA-resistant completion descriptors, exact CQ arithmetic, shared same/cross-tenant restore dedup, capability-safe MTP joins, ticket-scoped physical and waiter-scoped logical charges, cancellation/CQE order proofs, and catalog binding. The consolidated scheduler r3 CPU slice adds preallocated deterministic R0/R1/W0/W1 queues, read-reserved buffer/descriptor/CQ admission, independent new-W0 and accepted-W0 byte bounds, projected variable-read enforcement, one admission between service decisions, fail-closed maximum read sizing, and bounded R0 preference so continuous resume traffic cannot starve R1 restores. Durable-format candidate `96be26e` specifies exact segment/catalog/journal/checkpoint/control bytes, out-of-order child visibility, eviction, publication abort, relocation recovery, nonreused high-water marks, garbage reconstruction, and endurance buckets. Online-publication r2 replaces an estate-dependent catalog scan with a fixed-depth sparse-Merkle root while retaining every record size and offset. The Linux probe candidate now freezes a Rust-only single-issuer 16-SQ/32-CQ fixed-buffer/file feature matrix, real alignment/short-I/O boundaries, injectable syscall/completion faults, exact cancellation/CQE drain, and nonproduction path safety. All remain design or synchronous CPU evidence: no durable codec, registered memory, io_uring execution, cleaner, CUDA copies, or target-device evidence exists | Adversarially review the three CPU gates including scheduler r3, durable-format base, sparse-Merkle amendment, and Linux-probe design; after all dependencies pass, implement one coordinated codec/recovery matrix and the off-by-default nonproduction io_uring probe; qualify target storage, cleaner, and decode isolation last |
| K04 | REVIEW | Newly sealed runtime pages publish target plus draft sidecar durably and become reusable without restart | The retained no-write durable dedup/MTP-upgrade/collision matrix and prefix/residency coherence corrections are machine-accepted. Fable withheld the first online-publication token on four contract ambiguities. Corrective amendment `docs/online-prefix-publication-v1-r2.md` now defines namespace v2 with explicit target/indexer/draft-sidecar ABI identity, atomic retired-versus-live allocation lease classification, fork-safe committed-token cursors, a fixed-depth structurally shared runtime catalog, bounded parent healing, four-rank release, shutdown, re-offer, residency, and capacity semantics coordinated with the new durable format. Implementation remains absent. Corrections `d3ca693` and `6a5c574` reject missing journal history and propagate authoritative record identity into restored/active prefix attachments; dedicated reviews are pending | Fable r2 online-publication and durable-format verdicts, then one coordinated online CPU proof matrix |
| K05 | REVIEW | Prefix cache survives restart, corruption, torn journal, cache thrash, pinned pressure, and DCP posture changes end to end | Candidate `d0ac1d3` adds a deterministic integrated CPU lifecycle proof. Corrections through `a4bbfb0` bind restore identity, make admission/release transactions atomic, retain physical-operation quota after cancellation, fail-stop uncertain writes, reject multiple writers and same-key conflicts, retain exact revisions/MTP capability, reject missing/invalid journal history, transaction-sequence gaps, and catalog corruption, and preserve append-only allocation. Fable substantively accepted exact restore identity, but its prose-wrapped token is not machine-acceptable and is included in the four-result attestation repair request. Rank-residency r2 closes its Fable-withheld flaky-store gate with atomic unique test roots and binds every registration plan to a monotonic manager state generation. Restore-quota r2 makes impossible underflow release-visible and fail-closed, and retains coordinator-owned recovery state if failed-submission rollback preflight unexpectedly fails. Both exact filters passed fifty consecutive times. The remaining corrections await review, and no model/GPU/direct-I/O lifecycle exists | Repair the restore-identity result, rereview the bounded rank-residency and restore-quota corrections, and preserve the other CPU fixtures while implementing accepted K03/K04 and C05 paths; then run the integrated cold/warm fault matrix |
| K06 | OPEN | One live 1M request is admitted and executed without false active-capacity accounting or tier thrash | `f480ef1` proves a coordinator-owned CPU table at 1,048,575 positions for both MTP0 and MTP6-capable posture, exact 4,096-page/rank accounting, final-token execution, full cleanup, MTP tail fallback, and over-limit rejection. It uses test-only cached metadata, not model KV or live tiers | Rank delta/device arenas plus full fit-capable checkpoint and tier-thrash execution |

## P1 — concurrency and service

| ID | State | Required outcome | Current evidence / blocker | Next gate |
|---|---|---|---|---|
| S01 | PASS | CPU scheduler supports bounded continuous batching, weighted tenant fairness, prefix-pending admission, and collective-safe cancellation | The synchronous four-rank startup handshake and TP4 physical-step quota corrections are machine-accepted. Prior atomicity and ownership corrections remain; `f56e0bc` makes cancellation queue-independent, and `f480ef1` applies it before peer selection, releases active mappings/pins, dynamically bounds MTP depth by remaining output positions, and retains multi-tenant batching. The newer TP4 worker admission-state correction makes ordinary/exclusive permit corruption release-visible and fail-closed, reserves disjoint count/closed/poison/exclusive bits, and publishes terminal closure before result visibility. Pre-freeze stress removed one queue-vs-closure race and one invalid exact-ack-count assumption; the corrected 26-test worker filter passed 500 fresh processes. Its dedicated review remains pending | Rereview the TP4 admission-state correction, resolve the remaining CPU transaction reviews, then re-prove with the real executor |
| S02 | REVIEW | Bounded HTTP-to-coordinator adapter, tenant ownership, stop-safe streaming, slow-client isolation, and structured fatal drain accepted | Fable withheld the v2 backend token on two ownership defects already corrected by `fa8e3b3` and `6050d8e`: rejected event cancellation retains attribution for fatal drain, and retained admission rollback becomes fatal without losing owner/pending state. `docs/coordinator-api-backend-v3-proof.md` consolidates those fixes, replaces the review's 100 ms failure-test window with a four-rank/two-barrier schedule plus unwind guard, and explicitly limits structured-terminal guarantees only for a poisoned owner mutex classified as process-fatal. The final guarded schedule passed 100 fresh-process runs. Retained HTTP corrections `a7b1cc9`/`20c773c` and queue-independent cancellation `f56e0bc` remain; all require review | Pin and adversarially review the consolidated backend v3 bytes and retained HTTP corrections, then preserve this control while implementing the accepted nonblocking transport |
| S03 | REVIEW | Host request/step observability has correct clocks, counts, MTP ordinals, graph routes, no metric-recording allocation, and consistent concurrent lifecycle totals | Metrics candidate `9607aa0`; backend concurrency delta `8aaef8e`; combined v2 handoff pending | Adversarial verdict and fixes |
| S04 | REVIEW | Exact probabilistic parameters and deterministic seed/RNG state reach rank execution and responses | Sampling/RNG candidate `7c71818`; StepInput CPU ABI `d1f9825` carries exact fields; integration `e1d51ce` retains, constructs, and delivers them to all ranks and the backend materializes exact greedy seeds. Non-greedy remains rejected because `StepOutput.v2` final-counter consensus/commit is absent | Review contracts and delivery, implement output-counter consensus/commit, then remove the rejection |
| S05 | REVIEW | Nonblocking network transport sustains target concurrency with bounded memory | Fable withheld the first design token after identifying a real lost-wakeup window in the release-clear/acquire-recheck eventfd protocol. Corrective amendment `docs/nonblocking-http-transport-v1-r2.md` requires SeqCst notification ownership RMWs plus a weak-memory model proof, exact 24-bit-slot/40-bit-generation epoll cookies with full slab validation, request-local pre-accept overflow cleanup, health-before-address publication, zero-timeout epoll while locally runnable, Linux-only execution evidence, proxy identity in the configuration digest, and a frozen 120,000-request S05 CPU control. Implementation remains intentionally absent | Adversarially review the r2 amendment, then implement the accepted parser/reactor ABI and exhaustive wake model before Linux loopback and sustained controls |
| S06 | REVIEW | Admission enforces per-tenant queued-token, resident-KV, and context-band limits | Contract candidate `7e810c4` defines a single permit ledger, authenticated ingress, global-physical/tenant-logical charges, restore/step/offload transactions, exact 1M/page-slack arithmetic, and fatal cleanup; implementation is intentionally absent | Fable review of `docs/fable-tenant-resource-quotas-v1-handoff.md`, then CPU proof |
| S07 | REVIEW | Sustained multi-user, cache-thrash, cancellation, rank-fault, and slow-client tests pass | `docs/sustained-serving-load-fault-v1.md` freezes one black-box Rust driver, deterministic open/closed-loop schedules, CPU and SM120 matrices, generation-bound faults, raw per-request evidence, resource ceilings, and six-hour soak. Implementation and all run evidence remain absent | Adversarial design review, then deterministic schedule/corpus CPU reference and bounded driver |

## P1 — hardware evidence and performance

| ID | State | Required outcome | Current evidence / blocker | Next gate |
|---|---|---|---|---|
| H01 | REVIEW | Re-inventory cn4 and reproduce the current source/toolchain/test state without disturbing other workloads | Final read-only inventory found 4×SM120 and an existing four-rank vLLM allocation; no GLMAXX process or launch occurred, `/home/derek/glmaxx` was not a Git worktree, and cn4 was released. The new environment-capture design freezes a Rust-owned no-shell allowlist, exact source/executable/container/toolchain/host/firmware/PCIe/NUMA identity, before/after occupancy, secret exclusions, immutable external evidence, and idle/occupied/unstable verdicts. It is unreviewed and unimplemented; this repository has no current cn4 authorization | Adversarially review the capture design, implement and CPU-prove its parsers/allowlist, then seek a renewed read-only inventory window followed separately by no-GPU CPU reproduction |
| H02 | AUTH | Actual-shape NVFP4 and EXL3 correctness/performance matrix on all required row buckets | Prior records prove `sm_120f` cubins and ABI only | H01 plus C01 |
| H03 | AUTH | PCIe TP/DCP routes measured pairwise and collectively on the actual topology | Route compiler is CPU-only | H01, then NCCL controls and deterministic alternatives |
| H04 | OPEN | One complete sparse-layer TP4 replay matches reference activations/logits | No replay artifact. The base M4 design is superseded by `docs/small-checkpoint-runner-v1-r2.md`, which corrects source/native-output identity, laboratory plan/header/arena binding, owner-thread-before-adoption ordering, stale warm-output detection, and the recoverable-versus-process-fatal cleanup boundary. It retains layer 6 plus final norm/head, 533 tensors and 1,982,245,376 payload bytes per rank, real captured M3 boundaries, and no full-model claim. `docs/nvfp4-laboratory-manifest-v1.md` supplies the exact 130,944-byte aligned device metadata arena and a type state that cannot reach service health. Both are unreviewed designs | Adversarially review M4 r2 plus its laboratory manifest, then C06–C08 and H03 for M3 before implementing the CPU/mock runner and later authorized SM120 execution |
| H05 | OPEN | Graph-captured prefill/decode/MTP buckets pass repeated correctness and timing | CPU candidate `9bdb208` now chunks configured prefill work to the highest-work legal captured entry instead of stalling on a wider uncaptured shape. Design candidate `9b04652` addresses both discovered ABI blockers: the unique-key collision for multiple prefill chunks and the incorrect global 448-row cap that rejects the planned 3,072-row control. No v2 implementation or CUDA graph exists | Fable reviews of `docs/fable-prefill-captured-shape-v1-handoff.md` and `docs/fable-prefill-graph-profile-abi-v2-handoff.md`, then implement/prove v2 before C05/H02 capture/eager comparison |
| H06 | REVIEW | Matched end-to-end matrix reports TTFT, ITL p50/p95/p99, throughput, useful-token throughput, capacity, failures, cold/warm prefix, context, concurrency, and MTP depth | `docs/matched-runtime-control-v1.md` now freezes the common comparison key, raw request records, metric boundaries, paired-block statistics, and fail-closed verdicts. It is a design candidate only; the sustained-serving driver remains unimplemented | Adversarially review both benchmark designs, then implement the shared CPU-only schemas/comparator and mock connectors after the earlier correctness/quality gates |
| H07 | REVIEW | Pinned vLLM, SGLang, llama.cpp/ExLlama, BF16/FP8, and existing-runtime controls are run under matched precision/cache/batch/context | The matched-control design defines immutable runtime/capability receipts, precision-membership and logical-weight identity, exact tokenizer/streaming/cache/MTP equivalence, and explicit unavailable/product-posture classes. It deliberately does not select revisions or claim full-model BF16/FP8/NVFP4 fit | Adversarial design review, CPU connector proof, then operator-selected immutable control pins and separately authorized target runs after H06 |

## P2 — reproducibility and delivery

| ID | State | Required outcome | Current evidence / blocker | Next gate |
|---|---|---|---|---|
| D01 | PASS | Local format, tests, Clippy, FFI checks, deterministic fixtures, review provenance, and pinned tokenizer proof pass | The current complete local gate passes 399 Rust tests, workspace formatting, Clippy with warnings denied, CUDA-FFI host checks, deterministic fixture comparisons including EXL3 warp staging and both existing direct-tier CPU proofs, C/C++17 executor-ABI layout checks, engine/serving/cache lifecycle proofs, and all 117 current handoffs. The provenance gate machine-accepts 8/99 configured results with zero withheld. The batch staging verifier independently classifies 8 ready and 11 rejected artifacts in the current operator-owned inbox while leaving 80 configured results absent; it changes no acceptance count. The backend guarded fatal schedule separately passed 100 fresh processes. The external tokenizer proof was skipped because its directory variable was unset and its pinned fixture is unchanged. This host has no `nvcc`; no GPU evidence is implied | Keep green at every milestone |
| D02 | REVIEW | Current cn4 environment record pins source, container, driver, firmware, topology, clocks, occupancy, toolchains, and commands | `docs/cn4-release-20260729.md` pins the final GPU/driver/topology/occupancy observation, but source, container, firmware, clocks, and toolchains were not established because another workload already occupied the host. `docs/cn4-environment-capture-v1.md` now specifies the missing common evidence transaction and secret-safe command boundary, but has no review, implementation, or run | Review and implement the H01 capture, then produce and independently verify one current immutable cn4 record |
| D03 | OPEN | Exact build, conversion, deployment, serving, recovery, and benchmark commands reproduce production | Preparation scripts exist; no production server/deployment | Complete relevant implementation, then freeze commands |
| D04 | OPEN | Immutable results index covers every accepted CPU, GPU, quality, capacity, and benchmark artifact | Fail-closed v2 verifier proves candidate hashes for all 117 current handoffs and machine-accepts 8/99 configured results, automatically ingests declared result paths when they appear, and rejects self-review or token-only acceptance artifacts that omit candidate/input attestations. Its batch staging verifier now reports every ready, rejected, absent, and unmatched inbox artifact without promotion. The v3 current-tree binding design is pending review; most required result artifacts still do not exist | Accept and implement v3, then append only provenance-complete records and supply each review artifact explicitly |
| D05 | OPEN | Full-checkpoint conversion is allowed only after policy fit and quality gates pass | Profile correctly blocks conversion today | Q01/Q02 and measured HBM budget |

## External state

- cn4 GPU work is not currently authorized. Do not connect or launch until a
  new operator authorization is given. The final authorized inventory found
  a four-rank vLLM job already occupying all four GPUs.
- The user has stated that NVFP4 and EXL3 checkpoints already exist on the
  servers. No checkpoint download is required. Their current paths and hashes
  must be re-inventoried under H01; model bytes remain outside Git.
- Public quality-source revisions and the deterministic reasoning/coding/tool
  selections are pinned in the non-runnable
  `manifests/quality-corpus-sources-v1.json` design candidate. Generated
  corpora, gated FLORES+ content hashes, tokenized windows, the materialized
  corpus manifest, and the evaluator are still absent.
- Fable responses are required for every row marked `REVIEW`; their absence
  does not prevent independent CPU design or test work on unrelated rows.
