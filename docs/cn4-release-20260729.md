# cn4 release record

Date: 2026-07-29

Status: operator-requested release; no GLMAXX CUDA work launched

GPU claim: none

## Outcome

The operator granted a final cn4 window only to finish blocking device work
and then release the host to another development workload. The three
corrected Phase-B routes still require independent r2 acceptance artifacts:

- `fable-manifest-abi-v022-r2.md`;
- `fable-exl3-source-projection-v1-r2.md`; and
- the accepted EXL3 warp-decode route decision.

None of those acceptance results exists in this repository. Bypassing their
fail-closed checks would violate the required
design-review-before-device-execution sequence.

A read-only inventory at `2026-07-29T18:16:11+00:00` found an existing
four-rank vLLM job:

| Rank | Compute process | GPU memory |
|---:|---|---:|
| 0 | `VLLM::Worker_TP0_DCP0`, PID 886213 | 13,126 MiB |
| 1 | `VLLM::Worker_TP1_DCP1`, PID 886214 | 13,206 MiB |
| 2 | `VLLM::Worker_TP2_DCP2`, PID 886215 | 13,126 MiB |
| 3 | `VLLM::Worker_TP3_DCP3`, PID 886216 | 13,206 MiB |

All four GPUs were RTX PRO 6000 Blackwell Workstation Edition devices with
compute capability 12.0 and driver `595.71.05`. GPU utilization was zero at
the inventory instant, but the listed allocations made the host occupied.
No `glmaxx`, Cargo, nvcc, or CMake process was present.

The inventory did not stop a process, clear shared memory, create a CUDA
context, modify `/home/derek/glmaxx`, or launch a kernel. The SSH session was
closed immediately after the checks. cn4 is released; a future connection or
launch requires a new explicit operator authorization.

## Final release confirmation

At the operator's final release request, a second ownership-scoped check at
`2026-07-30T00:48:46+00:00` found no `glmaxx` or `glm5-native` process and no
`derek`-owned `/dev/shm` entry whose name contained `glmaxx`. No project
cleanup action was therefore required.

The unrelated vLLM job had advanced to the following full-device
allocations:

| Rank | Compute process | GPU memory |
|---:|---|---:|
| 0 | `VLLM::Worker_TP0_DCP0`, PID 910723 | 95,496 MiB |
| 1 | `VLLM::Worker_TP1_DCP1`, PID 910724 | 95,572 MiB |
| 2 | `VLLM::Worker_TP2_DCP2`, PID 910725 | 95,496 MiB |
| 3 | `VLLM::Worker_TP3_DCP3`, PID 910726 | 95,632 MiB |

Those processes were not signalled or otherwise disturbed. The final session
performed no build, test, allocation, context creation, file mutation, or
kernel launch on cn4 and was closed after the read-only check.

## Commands

The read-only session used:

```text
date --iso-8601=seconds
hostnamectl --static
nvidia-smi --query-gpu=index,name,uuid,pci.bus_id,compute_cap,driver_version,memory.total,memory.used,utilization.gpu --format=csv,noheader
nvidia-smi --query-compute-apps=gpu_uuid,pid,process_name,used_gpu_memory --format=csv,noheader
nvidia-smi topo -m
ps -eo pid,user,comm,args
```

The final confirmation used:

```text
hostname
date -Is
nvidia-smi --query-compute-apps=pid,process_name,used_memory --format=csv,noheader
ps -eo pid,user,etimes,args
find /dev/shm -maxdepth 1 -user derek -iname "*glmaxx*"
```

`/home/derek/glmaxx` existed but was not a Git worktree, so no source
revision was inferred from that directory.
